---
platform: linux

image_resource:
  type: docker-image
  source: { repository: concourse/bosh-cli }

inputs:
  - name: resource-pipeline
  - name: target-repo

outputs:
  - name: updated-gist

run:
  path: resource-pipeline/commit.sh
